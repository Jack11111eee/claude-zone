#!/usr/bin/env python3
"""zoning plugin hook stub: SessionStart.

Placeholder for phase 01-01; real logic (prefix parse -> index register ->
pending probe -> stdout hint) lands in 01-02 via `zone` bin internal
subcommands. Hook stays a thin wrapper by design (DESIGN.md §9 / M-5).
"""
import sys

sys.exit(0)
