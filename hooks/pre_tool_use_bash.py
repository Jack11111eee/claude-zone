#!/usr/bin/env python3
"""zoning plugin hook stub: PreToolUse (Bash).

Placeholder for phase 01-01; real logic (zone guards: bash_block_patterns
deny, e.g. maint high-risk git forms) lands in 01-03 via `zone` bin internal
subcommands (DESIGN.md §9 / §8.2).
"""
import sys

sys.exit(0)
