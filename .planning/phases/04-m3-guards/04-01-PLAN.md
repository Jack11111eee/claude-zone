---
phase: 04-m3-guards
plan: 01
type: execute
wave: 1
depends_on: ["01-03", "02-01"]
files_modified:
  - hooks/pre-tool-use-write.py
  - hooks/hooks.json
files_deleted: []
coupling_justified: []
autonomous: true
requirements: [GUARD-01, GUARD-03]
user_setup: []
must_haves:
  truths:
    - "discuss 会话调 Write/Edit 于 handoffs/ 外路径 → PreToolUse deny（decision: block），拒绝消息含区名与出路（写 handoff 或换区）"
    - "同会话写 ~/.claude/zoning/<slug>/handoffs/** → 放行（allow_paths glob 命中）"
    - "maint 会话写 .gitignore、.claude/zones/** → 放行；写源码 → deny"
    - "unzoned 会话（索引无此 session）→ 一律放行（degrade-to-open，GUARD-03）"
    - "判区 by sessionId（stdin session_id → 索引查区 → 该区合并定义 guards）"
  artifacts:
    - hooks/pre-tool-use-write.py
  key_links:
    - "guard 求值走 `zone _check-guard write --path <p> --session <id>`（bin 内部子命令，M-5）——hook 只把结果翻译成 PreToolUse decision JSON"
    - "glob 匹配：** 跨目录、* 单段，纯 stdlib fnmatch 不够（** 需自实现 ~15 行）"
---

<objective>
Write/Edit 护栏（GUARD-01/03）。

Purpose: discuss 区"禁止修改源码"从 prompt 里的自觉变成机制上的墙（威慑级）。
Output: pre-tool-use-write.py + bin 侧 _check-guard write。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

设计权威：
@DESIGN.md

涉及节：§8（guards 值表 H-1、B-4）、§9（PreToolUse 行+degrade-to-open）、§7.3（判区 by sessionId）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: zone _check-guard write（bin 侧求值）</name>
  <files>bin/zone</files>
  <read_first>DESIGN.md §8 H-1 表、§9</read_first>
  <action>
`zone _check-guard write --path <p> [--session <id>]`：session → 索引查区（无 → unzoned 放行）→ 区合并定义 guards.deny_write_edit == false → 放行；true → path 对 allow_paths 逐模式 glob 匹配（** 跨段）→ 命中放行，否则拒绝。输出 JSON {decision: allow|deny, zone, pattern?}。exit code：allow 0 / deny 0（决策走数据不走退出码——hook 消费 JSON）。
glob：实现 match_glob(pattern, path)：** 任意跨段、* 段内任意、其余字面。路径先规范化（~ 展开、./ 去除）。
  </action>
  <verify>矩阵测试：discuss×源码（deny）、discuss×handoffs（allow）、maint×.gitignore（allow）、maint×src（deny）、core×任意（allow）、unzoned×任意（allow）</verify>
  <acceptance_criteria>
    - "六象限矩阵全部正确"
    - "** 匹配跨目录（handoffs/a/b/c.md 命中 **/handoffs/**）"
  </acceptance_criteria>
  <done>guard 判定逻辑单一实现在 bin</done>
</task>

<task type="auto">
  <name>Task 2: hook 接线 + deny 话术</name>
  <files>hooks/pre-tool-use-write.py, hooks/hooks.json</files>
  <read_first>DESIGN.md §9</read_first>
  <action>
薄封装：读 stdin（tool_name/tool_input.file_path/session_id）→ 调 `_check-guard write` → deny 时输出 PreToolUse decision JSON（permissionDecision: deny → hookSpecificOutput.permissionDecision，照官方 hook 规范字段名；reason 含："讨论区护栏：本区禁止修改源码。请用 /handoff 交接或 /zone 换区。写 handoff 文档不受限。"——按区名动态）。allow → exit 0 无输出。hooks.json 把 PreToolUse (Write|Edit) 指到本文件。
  </action>
  <verify>printf 构造 stdin 六象限测试；deny 输出的 JSON 能被 jq 解析</verify>
  <acceptance_criteria>
    - "deny JSON 含 permissionDecision: 'deny' 与中文话术"
    - "allow 静默"
  </acceptance_criteria>
  <done>护栏 1/2 类落地</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 六象限 + hook JSON 双过
- [ ] 真会话一次：discuss 会话让模型改文件，观察被拦
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- unzoned 永不被拦（GUARD-03）
</success_criteria>

<output>
After completion, create `.planning/phases/04-m3-guards/04-01-SUMMARY.md`
</output>
