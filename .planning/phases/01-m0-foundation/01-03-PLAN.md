---
phase: 01-m0-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - bin/zone
files_deleted: []
coupling_justified: []
autonomous: true
requirements: [ZONE-03, STORE-01]
user_setup: []
must_haves:
  truths:
    - "zone which --session <id> 查索引返回该会话现区（最后一条 session 事件）"
    - "zone list 渲染表格：id、状态、标题、from→to、相对时间（M0 只须渲染会话条目与 handoff 汇总行）"
    - "index.jsonl 落在 ~/.claude/zoning/<project-slug>/，slug 规则=非字母数字→-（同 ~/.claude/projects）"
    - "并发 append 安全：单行 JSON、无锁、损坏行不崩（M0 容忍即可）"
  artifacts:
    - bin/zone（which/list/_register-session 实装）
  key_links:
    - "_register-session 供 01-02 hook 调用（M-5 薄封装的 bin 侧）"
    - "事件 schema 与 §7.2 逐字段一致（v/ts/type/sessionId/zone/title/cwd/source）"
---

<objective>
sidecar 索引引擎与查询面：`zone _register-session` + `which` + `list`。

Purpose: 索引是全部机制的存储底座（判区/巡视/pending 都靠它）；不落库，一切上层是无源之水。
Output: bin/zone 内索引模块 + 三个子命令实装。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

设计权威：
@DESIGN.md

涉及节：§7.1（布局与 slug）、§7.2（事件 schema）、§7.3（which 查询）、§5（契约表+附录 A）、§3（index 事件 source 语义）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 索引引擎（append/open/重放）+ _register-session</name>
  <files>bin/zone</files>
  <read_first>DESIGN.md §7.1、§7.2</read_first>
  <action>
实现路径解析：home 下 `.claude/zoning/<slug>/index.jsonl`，slug= cwd 绝对路径中非 [A-Za-z0-9] 字符逐个替换为 -（同类 ~/.claude/projects 规则）。目录不存在则建（含 handoffs/ 与 handoffs/.trash/）。事件行=v/ts/type/sessionId/zone/title/cwd/source（§7.2 字段逐字）。`zone _register-session`（内部子命令，`_` 前缀不进 --help 顶层描述）：stdin JSON（session_id/session_title/cwd/source）→ title 解析（复用 title 逻辑）→ 有区才 append session 事件，unzoned → 零动作 exit 0。索引读取：逐行 json.loads，损坏行跳过收集（doctor 消费），同 key（sessionId）最后一条即现值。
  </action>
  <verify>ZONING_HOME=$(mktemp -d) 环境（若 bin 支持则加 --cwd 与 override env；不支持测完人工指到真实 home 再验一次）；echo stdin 三连：--core x、无前缀、[discuss] 旧 → 检查 index.jsonl 行数与字段</verify>
  <acceptance_criteria>
    - "_register-session 对 --core x 产出 {'v':1,…,'type':'session','zone':'core','source':'prefix'} 行"
    - "unzoned 输入不追加任何行"
    - "同一 sessionId 二次登记 → 两行事件，list/which 取最后一条"
    - "手工破坏一行 JSON 后，which/list 仍正常输出（跳过坏行）"
  </acceptance_criteria>
  <done>索引引擎+登记命令可用且 schema 对齐 §7.2</done>
</task>

<task type="auto">
  <name>Task 2: which + list 实装（含 --json）</name>
  <files>bin/zone</files>
  <read_first>DESIGN.md §5 契约表、§7.3、附录 A</read_first>
  <action>
`zone which [--session <id>] [--cwd <path>]`：无 --session 时读环境 CLAUDE_CODE_SESSION_ID（V-8 已验存在；缺则 exit 2 提示）。返回该 session 现区或 unzoned。`zone list [--zone=Z]`：重放索引，会话条目（zone/title/相对时间）+ handoff 条目（M0 handoffs 为空属正常，渲染表头即可）。表格列照 §5：id、状态、标题、from→to、相对时间。--json 输出附录 A 契约。
  </action>
  <verify>python3 bin/zone which --session <登记过的id>；python3 bin/zone list；均再 --json 复测</verify>
  <acceptance_criteria>
    - "which 返回 core（对登记过的 session）"
    - "which 对未知 session 返回 unzoned"
    - "list 无 handoff 时不报错，渲染会话表"
    - "--json 输出含 ok/data 键"
  </acceptance_criteria>
  <done>which/list 对齐 §5 契约与附录 A</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 登记三个会话（core/discuss/无前缀）后 zone list 显示两条会话行
- [ ] which --session 正确返回
- [ ] 损坏行容忍
- [ ] 纯标准库（grep -E '^\s*import|^from' bin/zone 无第三方）
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- 索引文件单行事件、append-only（无 update-in-place）
</success_criteria>

<output>
After completion, create `.planning/phases/01-m0-foundation/01-03-SUMMARY.md`
</output>
