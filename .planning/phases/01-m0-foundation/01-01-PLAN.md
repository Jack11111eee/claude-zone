---
phase: 01-m0-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/plugin.json
  - hooks/hooks.json
  - bin/zone
  - zones/chore.yaml
  - zones/core.yaml
  - zones/discuss.yaml
  - zones/maint.yaml
files_deleted: []
coupling_justified: []
autonomous: true
requirements: [ZONE-03]
user_setup: []
must_haves:
  truths:
    - "python3 bin/zone title '--core 测试' 输出 zone: core 及任务名"
    - "python3 bin/zone title '随便聊聊' 输出 unzoned"
    - "python3 bin/zone title '[core] 旧语法' 归一化为 zone: core"
    - "python3 bin/zone --help 不报错；各子命令 --json 输出含 ok/v/cmd/data 键"
  artifacts:
    - bin/zone
    - .claude-plugin/plugin.json
    - hooks/hooks.json
    - zones/chore.yaml
    - zones/core.yaml
    - zones/discuss.yaml
    - zones/maint.yaml
  key_links:
    - "plugin.json name=zoning（安装标识）；hooks.json 引用的 hook 脚本在本 phase 为占位（实现在 01-02/01-03）"

---

<objective>
插件脚手架 + `zone` bin 公开命令骨架（title 归一化/解析）。

Purpose: 一切机制的地基——plugin.json 使 Claude Code 识别插件，hooks.json 声明事件接点，bin/zone 提供 M-5 要求的"同一逻辑单一实现"宿主。
Output: 可安装的插件骨架；`zone title` 可用；四区 yaml 定义就位。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

设计权威（实现细节逐条照办）：
@DESIGN.md

涉及节：§5（bin 契约表+附录 A）、§8.1（脚手架定稿 B-7）、§3（title 命名语法与归一化）、§2（四区表）、§8（zone.yaml 规格与四区 guards 值表 H-1）、§8.2（maint 黑名单 L-6）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 插件脚手架（plugin.json + hooks.json + 目录树）</name>
  <files>.claude-plugin/plugin.json, hooks/hooks.json</files>
  <read_first>DESIGN.md §8.1, DESIGN.md §9</read_first>
  <action>
按 §8.1 定稿创建目录树。`.claude-plugin/plugin.json`：name "zoning"、description 一句话（功能分区）、版本 0.0.1。`hooks/hooks.json` 按 §9 清单声明五个 hook 事件：SessionStart（matcher startup/resume/clear/compact 按文档支持值写全）、UserPromptSubmit（all）、PreToolUse×2（Write|Edit 一个、Bash 一个）、SessionEnd（空 matcher）。每个 hook 指向的命令先用占位脚本（hooks/ 下，`python3 bin/zone _hook-session-start` 风格的薄封装——本 plan 只建占位 stub，内含 exit 0；01-02/01-03 填实）。hooks.json 的 command 用 "$CLAUDE_PLUGIN_ROOT" 前缀引用插件内路径（可移植）。
  </action>
  <verify>cat .claude-plugin/plugin.json | python3 -m json.tool；cat hooks/hooks.json | python3 -m json.tool（两个 JSON 均解析通过）</verify>
  <acceptance_criteria>
    - ".claude-plugin/plugin.json 含 \"name\": \"zoning\""
    - "hooks/hooks.json 含 SessionStart、UserPromptSubmit、PreToolUse、SessionEnd 四类事件且 PreToolUse 出现两次"
    - "hooks.json 的 command 均引用 $CLAUDE_PLUGIN_ROOT"
  </acceptance_criteria>
  <done>两个 JSON 合法且结构符合 §8.1/§9</done>
</task>

<task type="auto">
  <name>Task 2: bin/zone 基础（argparse 骨架 + title 子命令 + --json 契约）</name>
  <files>bin/zone</files>
  <read_first>DESIGN.md §5（契约表）、§13（附录 A --json 契约）、§3（命名语法）</read_first>
  <action>
纯 Python 标准库单文件（shebang python3，chmod +x）。argparse 子命令骨架：which/list/pending/register/snapshot/inject/gc/doctor/show/title 全部声明（本 plan 只实现 title 与公共框架，其余子命令接受参数但打印 not-implemented 错误 exit 2，后续 plan 填实）。全局 --json 旗标。附录 A 契约：成功 {"ok":true,"v":1,"cmd":<cmd>,"data":{…}}，失败 {"ok":false,"v":1,"cmd":<cmd>,"error":"…","exit":N}。退出码 0/2/3/4。
title 逻辑照 §3 与 §5：输入 title 字符串 → (a) `--<zone> ` 前缀恰匹配四区之一 → 输出 {zone, rest}；(b) 残留 `[zone] ` 旧语法 → 归一化（zone: core, rest 任务名，normalized: true）；(c) 其余 → unzoned。--json 时 data={zone,rest,normalized}。
  </action>
  <verify>python3 bin/zone title '--core 测试'；python3 bin/zone title '普通标题'；python3 bin/zone title '[core] 旧'；python3 bin/zone title '--nope x'（须 unzoned——zone 名必须恰为四区之一）；python3 bin/zone title '--core 测试' --json</verify>
  <acceptance_criteria>
    - "python3 bin/zone title '--core 测试' 输出 zone: core, rest: 测试"
    - "python3 bin/zone title '[core] 旧' 归一化输出 zone: core 且 normalized=true"
    - "python3 bin/zone title '--nope x' 输出 unzoned"
    - "--json 输出含 ok/v/cmd/data 键"
  </acceptance_criteria>
  <done>title 全行为符合 §3/§5/附录 A</done>
</task>

<task type="auto">
  <name>Task 3: zones/*.yaml 四区内置默认定义</name>
  <files>zones/chore.yaml, zones/core.yaml, zones/discuss.yaml, zones/maint.yaml</files>
  <read_first>DESIGN.md §8（schema + H-1 四区 guards 值表）、§8.2（L-6 黑名单）、§2.1（prompt 定稿正文）</read_first>
  <action>
按 §8 schema 每区一个文件：name/display/prefix/prompt（§2.1 定稿正文逐字）/mode_hint/model_hint/inject_on_entry/guards（照 H-1 表：discuss deny_write_edit=true + allow_paths=["**/handoffs/**"]；maint 同表加 .gitignore 与 **/.claude/zones/** 与 §8.2 七条 bash_block_patterns；chore/core 空 guards）/handoff_out（第一版先按 §6.3 矩阵填：chore→core、core→maint、discuss→core、maint 留空）。
  </action>
  <verify>python3 -c "import yaml" 2>/dev/null || for f in zones/*.yaml; do python3 bin/zone show "$(basename $f .yaml)" 2>/dev/null; done（show 未实现前用肉眼校验缩进+字段名）</verify>
  <acceptance_criteria>
    - "zones/discuss.yaml 含 deny_write_edit: true 与 allow_paths 含 **/handoffs/**"
    - "zones/maint.yaml 含 bash_block_patterns 七条（git push --force / reset --hard / branch -D / clean -f / checkout -- / reflog expire / gc prune）"
    - "四文件 prompt 与 §2.1 逐字一致"
  </acceptance_criteria>
  <done>四区定义字段齐全、值与 H-1/§8.2/§2.1 一致</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python3 .claude-plugin/../bin/zone title '--core x' → zone: core
- [ ] python3 bin/zone title '[core] x' → 归一化 core
- [ ] python3 bin/zone title 'x' → unzoned
- [ ] python3 bin/zone title '--core x' --json → 合法 JSON 且 ok=true
- [ ] 两个 JSON（plugin/hooks）解析通过
- [ ] zones/*.yaml 四文件齐、guards 值与 H-1 表一致
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- bin/zone 无第三方 import（纯标准库）
</success_criteria>

<output>
After completion, create `.planning/phases/01-m0-foundation/01-01-SUMMARY.md`
</output>
