---
phase: 03-m2-handoff
plan: 04
type: execute
wave: 2
depends_on: ["02-02", "03-01"]
files_modified:
  - skills/handoff/SKILL.md
  - hooks/session-start.py
  - hooks/user-prompt-submit.py
files_deleted: []
coupling_justified: []
autonomous: true
requirements: [HANDOFF-01, HANDOFF-04, HANDOFF-05]
user_setup: []
must_haves:
  truths:
    - "讨论区会话 /handoff core：模型按模板写出 frontmatter 合规文档到 ~/.claude/zoning/<slug>/handoffs/core/ho-*.md，随后调用 zone register <file> 登记（写入=登记，R-2）"
    - "文档正文模板为 discuss→core 链路矩阵（Decisions + Open Questions + 范围边界），含 Intent/Snapshot/Next Steps 段"
    - "登记后（消费前）新 core 会话启动或 /zone core 换区时，提示行出现 '未消费 handoff：1) <标题> → /zone inject <id>'（提示≠注入，D-002）"
    - "/handoff 命令正文不写死链路（from 从注入的 zone prompt 知，to 从用户参数知）"
    - "handoff 写完时输出深链 claude-cli://open?q=/zone core&cwd=<url-encoded 项目绝对路径>（H-2）"
  artifacts:
    - skills/handoff/SKILL.md
  key_links:
    - "SessionStart/UserPromptSubmit 的 pending 探测走 zone pending（03-02 产物），hook 不自己扫目录"
    - "skill 正文引导模型的调用序列：Write 文档 → zone register → 输出深链给用户"
---

<objective>
/handoff skill + pending 探测提示行（HANDOFF-01/04/05）——M2 旗舰收口 = v0.1。

Purpose: 把 03-01/03-02 的机器能力接上自然语言面。这是用户第一次"感觉插件存在"的时刻。
Output: /handoff skill 文件 + 两 hook 的 pending 探测。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

设计权威：
@DESIGN.md

涉及节：§6（全节：触发/schema/链路矩阵/消费语义）、§5（R-2 写入=登记、M-2）、§3（时序图 pending 探测、H-2 深链）、§10（/handoff 命令行）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: skills/handoff/SKILL.md（写作指令+模板）</name>
  <files>skills/handoff/SKILL.md</files>
  <read_first>DESIGN.md §6.1、§6.2、§6.3、R-2</read_first>
  <action>
skill 正文＝给模型的写作指令：(a) 触发形态 /handoff [<zone>] ["附注"]；(b) from=当前区（hook 注入的 prompt 上下文）、to=参数目标区；(c) 模板选择按 §6.3 矩阵（discuss→core 用 Decisions+Open Questions+范围边界侧重）；(d) frontmatter 规范（id 由模型生成 ho-<yyyymmdd>-<4随机码>、from/to/created ISO8601）；(e) 文档直接写最终路径 ~/.claude/zoning/<slug>/handoffs/<to>/<id>.md（M-1，无移动语义）；(f) 写完立即运行 zone register <file>（R-2 原子）；(g) 输出给用户：registered <id> + 深链 claude-cli://open?q=/zone core&cwd=<encoded>（按 to 生成 q 参数）。附七段模板正文（Intent/Decisions/Open Questions/Snapshot/Why Escalated/Next Steps，§6.2）；Why Escalated 段注明仅升级链路使用。
  </action>
  <verify>肉眼核对 SKILL.md 覆盖 a-g 全部要点+模板七段</verify>
  <acceptance_criteria>
    - "SKILL.md 含 zone register 调用指令与最终路径写法"
    - "模板七段齐"
    - "深链格式与 §3 H-2 一致（q=/zone <to>、cwd URL 编码）"
  </acceptance_criteria>
  <done>模型的交接行为有完整指令面</done>
</task>

<task type="auto">
  <name>Task 2: 两 hook 的 pending 探测</name>
  <files>hooks/session-start.py, hooks/user-prompt-submit.py</files>
  <read_first>DESIGN.md §3（时序）、M-3</read_first>
  <action>
session-start.py：登记后若门区存在，调 `zone pending --zone=<区>` 非空 → stdout 提示行（§3 格式："核心区已就绪。未消费 handoff：1) <标题> → /zone inject <id>"）。user-prompt-submit.py：换区后同样探测（M-3：与 SessionStart 同口径）。hook 均为薄封装：探测逻辑在 pending 子命令（03-02），hook 只拼提示行文案。
  </action>
  <verify>构造一个 pending handoff 后用假 stdin 跑两个 hook → 各自输出提示行；清空后不输出</verify>
  <acceptance_criteria>
    - "有 pending：提示行含 id 与标题"
    - "无 pending：无提示行"
    - "hook 文件无扫描逻辑（调 zone pending）"
  </acceptance_criteria>
  <done>提示≠注入原则落地</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 完整人肉链路演练：discuss 会话写 handoff → register → 新 core 会话见提示 → zone inject → consumed
- [ ] /zone inject 后模型按文档 Next Steps 行动（真会话一次）
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- 全链路端到端 = v0.1 打 tag
</success_criteria>

<output>
After completion, create `.planning/phases/03-m2-handoff/03-04-SUMMARY.md`
</output>
