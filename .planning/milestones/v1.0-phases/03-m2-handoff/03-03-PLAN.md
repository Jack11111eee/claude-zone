---
phase: 03-m2-handoff
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "03-01", "03-02"]
files_modified:
  - bin/zone
files_deleted: []
coupling_justified: []
autonomous: true
requirements: [STORE-02, STORE-04]
user_setup: []
must_haves:
  truths:
    - "zone doctor 在 02-01 定义检查之上新增三类一致性检查：孤儿（提醒 re-register）、幽灵（报告+inject exit 3 联动）、命名错位（frontmatter.to≠目录名）"
    - "索引损坏行：doctor 跳过并报告（行号+摘要），不崩（L-2）"
    - "zone reindex 内部逻辑（保底）：从 handoffs/ 目录与索引重放重建合并视图——不暴露为 CLI 子命令，doctor 输出给出人肉修复指引"
  artifacts:
    - bin/zone（doctor 扩展）
  key_links:
    - "孤儿检查与 03-02 的 unregistered 行为同一数据通路"
    - "doctor --json data={problems:[{file,field,message}]}"
---

<objective>
doctor 完整体：定义检查 + 存储一致性三类 + 索引容错（STORE-02/04）。

Purpose: 纯文件哲学的代价是没有事务——doctor 就是替代事务的那双眼睛。
Output: doctor 全检查矩阵。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

设计权威：
@DESIGN.md

涉及节：§7.4（三类检查）、§7.2（L-2 并发容忍）、§5（doctor 契约+reindex 定位）、附录 A doctor 行。
</context>

<tasks>

<task type="auto">
  <name>Task 1: doctor 一致性扩展 + 索引容错</name>
  <files>bin/zone</files>
  <read_first>DESIGN.md §7.4、L-2</read_first>
  <action>
在 02-01 的 doctor 上加：(a) 孤儿：handoffs/ 下有文件但索引无该 id → problems 列出（message 提醒人 re-register，不代办）；(b) 幽灵：索引 pending/re_consumed 但文件不存在 → problems（message 说明 inject 将 exit 3）；(c) 命名错位：文件所在目录名 ≠ frontmatter.to → problems（metadata 以 frontmatter 为准，不影响读取）。索引读取层：逐行 parse 失败 → 跳过收集 {line: N, raw_prefix}（doctor 报告；which/list/pending 已在 01-03 容忍）。--json problems 数组照附录 A。
  </action>
  <verify>构造孤儿/幽灵/命名错位/坏行四场景 → doctor 输出四条 problems；修复后 OK</verify>
  <acceptance_criteria>
    - "四类问题各自在 problems 中出现且 message 可行动"
    - "无问题输出 OK"
    - "坏行不再让 doctor 崩"
  </acceptance_criteria>
  <done>doctor 覆盖全部设计矩阵</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 场景矩阵全过
- [ ] --json 契约核过
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- 不自动修复任何东西（机器不猜状态）
</success_criteria>

<output>
After completion, create `.planning/phases/03-m2-handoff/03-03-SUMMARY.md`
</output>
