---
phase: 03-m2-handoff
plan: 02
type: execute
wave: 1
depends_on: ["01-03", "03-01"]
files_modified:
  - bin/zone
files_deleted: []
coupling_justified: []
autonomous: true
requirements: [STORE-02]
user_setup: []
must_haves:
  truths:
    - "zone pending [--zone=Z]：目录扫描 handoffs/<Z>/（文件存在）∩ 索引状态（非 consumed/stale）→ 输出表：id、状态、标题、from→to、相对时间"
    - "孤儿（有文件无登记）：行尾如实标 (unregistered)，不做自动登记（H-3）"
    - "幽灵（有登记无文件）：不出现在 pending；doctor 报告；inject exit 3"
    - "stale = created 起算 14 天未消费（--stale-days 可改）：渲染时派生（状态列 stale 标注），不新增事件"
    - "zone list：再加 handoff 全量（含 consumed）分组渲染"
  artifacts:
    - bin/zone（pending 实装）
  key_links:
    - "真值双源：pending 行来自目录扫描，每行状态列查索引回填（§7.4）"
    - "frontmatter.to ≠ 所在目录名 → doctor 报告（不影响 pending 读取，metadata 以 frontmatter 为准）"
---

<objective>
pending 双源查询（STORE-02 / H-3 一致性）。

Purpose: "未消费 handoff" 提示行（M-3）与 /zones 巡视的数据源。双源一致性是设计的深水区，单独成 plan 精确实现。
Output: zone pending + list 的 handoff 视图。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

设计权威：
@DESIGN.md

涉及节：§7.4（全节）、§5（pending/list 契约）、§6.4（stale 派生 B-3）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: zone pending 实现</name>
  <files>bin/zone</files>
  <read_first>DESIGN.md §7.4</read_first>
  <action>
目录扫描 handoffs/<zone>/（--zone 省略扫四个区目录）得候选集 → 每文件读 frontmatter（id/created/to）→ 查索引该 id 最新事件状态：无登记=孤儿 pending (unregistered)；pending=正常行；consumed/re_consumed=排除；stale 判定：created+14d < now → 状态列标 stale。幽灵（索引 pending 但文件不在）不出现在行里（doctor 的事）。表格输出照 §5 列序。--json items 数组（附录 A：id,from,to,status,title,created,stale,unregistered）。
  </action>
  <verify>构造：2 个 pending、1 个 consumed、1 个孤儿、1 个 15 天前 created（stale）→ zone pending 应列 3 行（2 pending + 1 孤儿，孤儿行 unregistered，15天那行标 stale——若本来就是孤儿则二者叠加）</verify>
  <acceptance_criteria>
    - "pending 行= 目录∩非 consumed"
    - "孤儿行 (unregistered) 标注"
    - "stale 派生显示不写事件"
    - "--zone=core 只扫 core 目录"
  </acceptance_criteria>
  <done>双源真值表精确落地</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 五文档场景矩阵输出正确
- [ ] --json 契约核过
- [ ] 与 03-01 的 inject 状态机联动无矛盾（inject 后 pending 里消失）
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- 孤儿不自动登记（grep 确认无 re-register 隐式调用）
</success_criteria>

<output>
After completion, create `.planning/phases/03-m2-handoff/03-02-SUMMARY.md`
</output>
