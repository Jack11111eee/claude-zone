---
phase: 03-m2-handoff
plan: 01
type: execute
wave: 1
depends_on: ["01-03"]
files_modified:
  - bin/zone
files_deleted: []
coupling_justified: []
autonomous: true
requirements: [HANDOFF-02, HANDOFF-03]
user_setup: []
must_haves:
  truths:
    - "zone register <file>：frontmatter 含 id/from/to/created 且 from/to ∈ 四区∪unzoned → 索引追加 pending handoff 事件，stdout 'registered <id>'"
    - "frontmatter 缺字段或值非法 → 拒绝登记，stderr 指出缺什么，exit 2（闭环留模型侧：提示修复后重register）"
    - "文档已存在再 register 同一 id → 幂等拒绝（已有登记），提示已 registered"
    - "zone inject <id>：打印全文 + 索引追加 consumed 事件（by=sessionId）；立即再 inject → 默认拒绝 + exit 3 语义，--force → 追加 re_consumed"
    - "inject 的 sessionId 来自 --session 或 CLAUDE_CODE_SESSION_ID"
  artifacts:
    - bin/zone（register/inject/_read-handoff-fm）
  key_links:
    - "register 不移动文件（M-1：文档按 §6.2 直接写最终路径 handoffs/<to>/<id>.md）"
    - "事件流 §7.2 handoff 事件：register→{type:handoff,status:pending,title,from,to}；inject→{status:consumed,by,subtype:inject}；force→{status:re_consumed,subtype:inject --force}"
---

<objective>
handoff 生命周期引擎：register 登记 + inject 消费（HANDOFF-02/03 核心）。

Purpose: 旗舰链路的机器侧全部——写入=登记原子性、消费状态不回退，都在这两个子命令里。
Output: bin/zone 的 register/inject 实装 + frontmatter 解析。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

设计权威：
@DESIGN.md

涉及节：§5（register/inject 契约+R-2+R-5+M-1）、§6.2（schema v1）、§6.4（生命周期）、§7.2（事件 schema）、§6.5（消费语义、跨区 allowed B-1）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: frontmatter 解析 + zone register</name>
  <files>bin/zone</files>
  <read_first>DESIGN.md §6.2、§5</read_first>
  <action>
受限 frontmatter 解析（--- 分隔、flat 键值，tags 列表）。校验：id 存在且形如 ho-YYYYMMDD-xxxx；from/to ∈ {chore,core,discuss,maint,unzoned}（from 允许 unzoned，L-3；to 必须是四区——文件落 handoffs/<to>/ 下）；created 存在且 ISO8601 可解析。合法 → append handoff 事件 {v,ts,type:handoff,id,from,to,status:pending,title}（title 取正文 # 一级标题；无则取 id）。文件路径必须位于本 project slug 的 handoffs/ 下（防误登记别项目文档）。id 已在索引 → 幂等拒绝。--json data={id,path}。
  </action>
  <verify>构造合规/缺 from/to 非法/坏日期/id 重复四种文档依次 register，核对事件行与 exit code</verify>
  <acceptance_criteria>
    - "合规文档 registered ho-xxx（stdout 与索引双证）"
    - "缺 to → stderr 指明，exit 2，索引无行"
    - "重复 id → 幂等拒绝"
    - "title 从正文 # 标题提取"
  </acceptance_criteria>
  <done>register 校验/登记/幂等三态齐</done>
</task>

<task type="auto">
  <name>Task 2: zone inject（消费+--force 语义）</name>
  <files>bin/zone</files>
  <read_first>DESIGN.md §6.4、§6.5、R-5</read_first>
  <action>
`zone inject <id> [--force]`：查索引最新 handoff 事件 → pending：打印文档全文（读 handoffs/<to>/<id>.md；幽灵=有登记无文件 → exit 3 stderr）→ append consumed 事件（by=--session 或 env CLAUDE_CODE_SESSION_ID；缺 → exit 2）。已 consumed：默认拒绝（提示 --force）exit 3；--force：打印全文 + append re_consumed（subtype: inject --force）。跨区消费允许（B-1，by 如实记）。--json data={id,from,to,status,body}。
  </action>
  <verify>pending→inject（全文+consumed）→再 inject（拒绝）→inject --force（re_consumed）；全链检查 index.jsonl 事件序</verify>
  <acceptance_criteria>
    - "首次 inject 打印全文且索引变 consumed"
    - "二次 inject 默认拒绝 exit 3"
    - "--force 后索引 latest=re_consumed 且 subtype 正确"
    - "幽灵 id → exit 3"
  </acceptance_criteria>
  <done>生命周期 pending→consumed→re_consumed 与 R-5 语义精确一致</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 四种 register 边界全部正确
- [ ] inject 生命周期三阶段事件正确
- [ ] --json 契约（附录 A）核过
- [ ] 纯标准库
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- register 不移动/不写文件（只读+索引 append）
</success_criteria>

<output>
After completion, create `.planning/phases/03-m2-handoff/03-01-SUMMARY.md`
</output>
