---
phase: 01-m0-foundation
plan: 02
type: execute
wave: 3
depends_on: ["01-01", "01-03"]
files_modified:
  - hooks/hooks.json
  - hooks/session-start.py
files_deleted: []
coupling_justified: []
autonomous: true
requirements: [ZONE-01]
user_setup: []
must_haves:
  truths:
    - "claude -n '--core 测试' 新会话：index.jsonl 出现该 session 的 session 事件（zone: core, source: prefix）"
    - "无前缀 claude 新会话：无索引事件、无 stdout 提示（零打扰）"
    - "stdin 带 agent_type 的 subagent 场景（字段存在且非空）：hook 直接退出不登记"
  artifacts:
    - hooks/session-start.py
  key_links:
    - "hook 调 `zone _register-session`（01-03 的内部子命令）完成登记——薄封装 M-5"
    - "stdout 提示行格式：'<区名>已就绪。'；subagent/非 startup 源静默"
---

<objective>
SessionStart hook 实装：前缀解析 → 索引登记 → stdout 提示（M0 验收核心之一）。

Purpose: 主路径（cz core 任务名）的落点。没有它，命名前缀只是个标签。
Output: hooks/session-start.py + hooks.json 填实该事件。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
.planning/phases/01-m0-foundation/01-03-SUMMARY.md（_register-session 接口）

设计权威：
@DESIGN.md

涉及节：§3（启动时序）、§9（hooks 清单+L-1 main-agent 检查）、§5（bin 内部子命令 _register-session）、§7.1/7.2（索引布局/事件 schema）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: zone _register-session 内部子命令（01-03 若缺则在此补齐）</name>
  <files>bin/zone, hooks/session-start.py</files>
  <read_first>DESIGN.md §5（M-5 薄封装）、§7.2（事件 schema）、01-03-SUMMARY.md</read_first>
  <action>
确认 01-03 已提供 `zone _register-session`（stdin json → title 解析 → session 事件 append）。若接口形态不同，以 01-03 实现为准在此适配，不重写 bin 逻辑。
  </action>
  <verify>echo '{"session_id":"t1","session_title":"--core x","cwd":"<repo>"}' | python3 bin/zone _register-session 2>&1（回显登记结果，索引出现一行 v:1 session 事件）</verify>
  <acceptance_criteria>
    - "_register-session 消费 stdin JSON 并产出符合 §7.2 的 session 事件行"
    - "同一 session_id 重复调用：追加事件（同 key 后事件即现值），不报错"
  </acceptance_criteria>
  <done>登记链 hook→bin 通畅</done>
</task>

<task type="auto">
  <name>Task 2: hooks/session-start.py 实装 + hooks.json 接线</name>
  <files>hooks/session-start.py, hooks/hooks.json</files>
  <read_first>DESIGN.md §3（时序）、§9（清单）、L-1</read_first>
  <action>
薄封装（M-5）：读 stdin JSON → L-1 main-agent 检查（stdin.agent_type 存在且非主会话 → exit 0 静默）→ source 检查（仅 startup 登记；resume/clear/compact 跳过登记但可探测）→ 调 `zone _register-session`（bash: `$CLAUDE_PLUGIN_ROOT/bin/zone _register-session`，stdin 透传）→ 若解析出门区：stdout 输出一行提示 `核心区已就绪。`（§3 格式，暂无 handoff 提示——那是 M2 的 01-04/Phase 3）。unzoned → 零输出。hooks.json 把 SessionStart 的 command 从占位 stub 换成 session-start.py。
  </action>
  <verify>cd 本 repo && claude -n '--core m0验证' 起真会话人工核对 index.jsonl 与提示行；或用 /tmp/zoning-vtest 的 hook 测试法：echo 组 stdin 后 python3 hooks/session-start.py 直接看 stdout 与 exit code</verify>
  <acceptance_criteria>
    - "stdin {session_title:'--core x', source:'startup'} → stdout 含 '已就绪'、exit 0"
    - "stdin {session_title:'无前缀'} → 无 stdout 输出、exit 0、索引无新事件"
    - "stdin 含 agent_type:'some-subagent' → 直接 exit 0 不登记"
    - "hooks.json 的 SessionStart command 指向 hooks/session-start.py"
  </acceptance_criteria>
  <done>三条入口行为（前缀/无前缀/subagent）全部符合 §3/§9</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] printf '{"session_title":"--core 测试","source":"startup","cwd":"…"}' | python3 hooks/session-start.py → 提示行+登记
- [ ] 无前缀 → 无输出
- [ ] agent_type 存在 → 跳过
- [ ] 真会话 claude -n "--core m0验收" 端到端核对（人工一次）
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- hook 文件无业务逻辑（只解析 stdin + 调 bin）——M-5 可核
</success_criteria>

<output>
After completion, create `.planning/phases/01-m0-foundation/01-02-SUMMARY.md`
</output>
