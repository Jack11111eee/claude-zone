---
phase: 05-m4-chains
plan: 01
type: execute
wave: 1
depends_on: ["02-02", "03-01"]
files_modified:
  - bin/zone
files_deleted: []
coupling_justified: []
autonomous: true
requirements: [HANDOFF-07, STORE-03]
user_setup: []
must_haves:
  truths:
    - "zone snapshot：输出当前 git 现场 markdown 块（当前分支、dirty 文件列表、最近 5 条 log 单行摘要、分支拓扑摘要）——纯机器产物（subprocess 调 git），零 LLM 加工"
    - "zone gc [--stale-days=14]：扫描全 handoffs/，created+stale_days < now 且状态非 pending 或 pending 但过期的文档移入 .trash/，stdout 'moved N, kept M'（移动不删除）"
    - "gc 后 zone pending 不再列被移走文档；.trash/ 内容 ls 可见"
    - "非 git 目录跑 snapshot：输出说明块（'非 git 仓库，Snapshot 段为空'），不崩"
  artifacts:
    - bin/zone（snapshot/gc 实装）
  key_links:
    - "snapshot 输出格式与 /handoff skill 的 Snapshot 段嵌入指引对齐（M-2：原样嵌入不经 LLM 改写）"
    - "gc 判定用 created（B-3）不用文件 mtime"
---

<objective>
zone snapshot（机器现场）+ zone gc（卫生）（HANDOFF-07 存储面 / STORE-03）。

Purpose: 收尾链的"现场"必须机器生成（不经 LLM 改写，Move 6.3 承诺）；gc 盘活 14 天死文档。
Output: snapshot 子命令 + gc 子命令。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

设计权威：
@DESIGN.md

涉及节：§5（snapshot/gc 契约、M-2）、§6.4（stale 与 gc、B-3）、§7.1（.trash 布局）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: zone snapshot</name>
  <files>bin/zone</files>
  <read_first>DESIGN.md §5 M-2</read_first>
  <action>
subprocess 调 git（--no-pager）：`git branch --show-current`、`git status --porcelain`、`git log --oneline -5`、`git branch -a --format`（拓扑摘要：当前/远端/本地分支计数即可，勿过度）。拼 markdown 块：## Snapshot 标题 + 四小节。git 命令失败（非 repo）→ 输出 'Snapshot: 非 git 仓库' 一行。--json data={body}。
  </action>
  <verify>在本 repo 跑 zone snapshot 看输出块；在 /tmp（非 git）跑看不崩</verify>
  <acceptance_criteria>
    - "输出含分支名、porcelain 文件、5 条 log"
    - "非 git 目录优雅降级"
  </acceptance_criteria>
  <done>现场块机器可信</done>
</task>

<task type="auto">
  <name>Task 2: zone gc</name>
  <files>bin/zone</files>
  <read_first>DESIGN.md §6.4、B-3、§7.1</read_first>
  <action>
扫描四个区目录全部文档 → 读 frontmatter created + 索引状态 → 判过期：created+stale_days < now（不论 pending 与否，过期即移——B-3 语义以 created 起算）→ shutil.move 到 .trash/（同名冲突加尾缀 -1/-2）。输出 moved N, kept M。--json data={moved,kept}。不做任何删除。
  </action>
  <verify>构造 15 天前文档（手改 frontmatter created）+ 正常文档 → gc → 检查 .trash/ 与 moved 1, kept 1</verify>
  <acceptance_criteria>
    - "过期文档进 .trash/ 原名保留"
    - "不过期文档不动"
    - "stdout 'moved 1, kept 1' 语义正确"
  </acceptance_criteria>
  <done>宁可堆积不可误删——只移动验证过</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] snapshot 两环境（git/非git）
- [ ] gc 场景矩阵
- [ ] --json 契约
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- 无 rm/删除调用（grep bin/zone）
</success_criteria>

<output>
After completion, create `.planning/phases/05-m4-chains/05-01-SUMMARY.md`
</output>
