---
phase: 04-m3-guards
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - hooks/pre-tool-use-bash.py
  - hooks/hooks.json
files_deleted: []
coupling_justified: []
autonomous: true
requirements: [GUARD-02, GUARD-04]
user_setup: []
must_haves:
  truths:
    - "maint 会话 Bash command 命中七条黑名单正则任一 → deny + '维护区护栏：高危 git 操作需人工确认，请改用安全形态或换区执行'"
    - "`git push --force-with-lease` 不被拦（GUARD-04：保护性操作，黑名单正则须精确不含它）"
    - "非 maint 会话（含 unzoned）不查黑名单（黑名单仅 maint 区 guards 定义）"
    - "正则匹配对象=Bash tool 调用的 command 字符串整体"
  artifacts:
    - hooks/pre-tool-use-bash.py
  key_links:
    - "求值走 `zone _check-guard bash --command <s> --session <id>`——与 04-01 同构，bin 单一实现"
---

<objective>
Bash 护栏（GUARD-02/04）。

Purpose: maint 区"维护不破坏"——拦已知高危形态，精确放行保护性操作。
Output: pre-tool-use-bash.py + bin 侧 _check-guard bash。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

设计权威：
@DESIGN.md

涉及节：§8.2（L-6 七条正则逐条）、§8（guards 值表 maint 行）、§9。
</context>

<tasks>

<task type="auto">
  <name>Task 1: zone _check-guard bash + hook 接线</name>
  <files>bin/zone, hooks/pre-tool-use-bash.py, hooks/hooks.json</files>
  <read_first>DESIGN.md §8.2</read_first>
  <action>
bin：`_check-guard bash --command <s> [--session <id>]` → 判区 → 该区 guards.bash_block_patterns（正则 list，re.search 逐条）→ 命中 deny {decision, zone, pattern}。七条正则照 §8.2 抄（注意 `git branch\s+(-D|--delete --force)\b` 与 `git push\s+.*--force` 不能误伤 `--force-with-lease`：push 的模式匹配 --force 后面不能再跟 -with-lease——用 `--force(?!-with-lease)` 负向前瞻或等价手写检查，python re 支持 lookahead）。
hook 薄封装：stdin（tool_input.command/session_id）→ deny 输出 permissionDecision JSON（话术照 §8.2："维护区护栏：高危 git 操作需人工确认，请改用安全形态或换区执行"）。
  </action>
  <verify>测试组：'git push --force origin main'（deny）、'git push --force-with-lease origin main'（allow）、'git reset --hard HEAD~3'（deny）、'git branch -D feature/x'（deny）、'git clean -fd'（deny）、'git checkout main -- src/x.py'（deny）、'git reflog expire --expire=now --all'（deny）、'git gc --prune=now'（deny）、'git status'（allow）、core 会话跑 'git push --force origin main'（allow——非 maint 不拦）</verify>
  <acceptance_criteria>
    - "九条测试组全部符合预期（7 deny、force-with-lease allow、status allow；非 maint allow）"
  </acceptance_criteria>
  <done>黑名单精度验证过（含 force-with-lease 组合形态）</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 九条矩阵全过
- [ ] hook JSON 输出格式与 04-01 一致
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- 误伤率为零（--force-with-lease、普通命令全放行）
</success_criteria>

<output>
After completion, create `.planning/phases/04-m3-guards/04-02-SUMMARY.md`
</output>
