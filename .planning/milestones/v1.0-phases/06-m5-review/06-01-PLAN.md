---
phase: 06-m5-review
plan: 01
type: execute
wave: 1
depends_on: ["03-02", "03-04"]
files_modified:
  - skills/zones/SKILL.md
files_deleted: []
coupling_justified: []
autonomous: true
requirements: [REVIEW-01, REVIEW-03]
user_setup: []
must_haves:
  truths:
    - "/zones 输出按区分组：每区最近会话（title 简短）+ 未消费 handoff（id+标题）"
    - "给用户的复制命令一律等号形式：claude --resume=\"--core 任务名\"（V-3 修正；绝不输出空格形式）"
    - "handoff 写完输出的深链走 /zone inject（3 的 §3 已定），本文档不重复实现——/zones 只读不写"
  artifacts:
    - skills/zones/SKILL.md
  key_links:
    - "/zones 正文=渲染 zone list + zone pending 的调用指引（A-2 同哲学：skill 引导模型调 bin，不自持逻辑）"
---

<objective>
/zones 巡视 skill（REVIEW-01，深链格式复审 REVIEW-03）。

Purpose: 三入口的第三入口——人和插件之间的总控台。
Output: skills/zones/SKILL.md。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

设计权威：
@DESIGN.md

涉及节：§10（/命令清单 /zones 行）、§3（V-3 等号形式）、V-4（深链）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: skills/zones/SKILL.md</name>
  <files>skills/zones/SKILL.md</files>
  <read_first>DESIGN.md §10、§3（V-3）、§3 deep-link、03-02-SUMMARY.md</read_first>
  <action>
skill 正文（给模型的指令）：(a) 运行 `zone list --json` + `zone pending --json`（或非 json 分两次）→ (b) 按区分组渲染：每区标题行（区名+中文 display）、最近会话行（相对时间+title 截断 40 字）、未消费 handoff 行（id、标题、from→to）；(c) resume 命令样本一律等号形式 `claude --resume="--<zone> <title>"`（加粗警告：空格形式是 command bug，V-3）；(d) 深链形式（§3 H-2 修订版）供跨终端跳场景。/zones 自身只读：正文明确"不写任何文件、不 append 事件"。
  </action>
  <verify>grep 等号形式样本存在且无空格形式误导；跑一遍 zone list --json 确认 data 结构可用</verify>
  <acceptance_criteria>
    - "SKILL.md 含等号形式 resume 样本与警告"
    - "分组渲染指令明确"
    - "只读声明存在"
  </acceptance_criteria>
  <done>巡视入口指令面完整</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 真会话 /zones 一次看渲染效果
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- 零写操作
</success_criteria>

<output>
After completion, create `.planning/phases/06-m5-review/06-01-SUMMARY.md`
</output>
