---
phase: 02-m1-injection
plan: 02
type: execute
wave: 2
depends_on: ["01-02", "02-01"]
files_modified:
  - hooks/user-prompt-submit.py
  - hooks/hooks.json
  - skills/zone/SKILL.md
files_deleted: []
coupling_justified: []
autonomous: true
requirements: [PROMPT-01, PROMPT-02, PROMPT-03]
user_setup: []
must_haves:
  truths:
    - "主路径会话（-n '--core x'）的 SessionStart additionalContext 含 core zone prompt §2.1 定稿全文"
    - "会话内 /zone core：UserPromptSubmit hook 追加 source:command 索引事件 + additionalContext 注入 core prompt + 提示未消费 handoff 行（M0 阶段 handoffs 为空，提示行自然省略）"
    - "sessionTitle 修订：hook 从 transcript 首条用户消息取名称（~40 字去换行），取不到用 '--core <MM-DD>'，经 zone title 归一化后写入"
    - "/zone 不带参数：不匹配 hook 正则，落到 skill 正文（占位说明）"
    - "skills/zone/SKILL.md 存在（V-5：命令文件存在是 UserPromptSubmit 触发前提）"
  artifacts:
    - hooks/user-prompt-submit.py
    - skills/zone/SKILL.md
  key_links:
    - "Prompt 注入统一走 'zone _zone-prompt <zone>' 内部子命令取各区 prompt 文本，hook 不复制 prompt 内容（M-5 单一实现）"
    - "换区=追加事件；同 sessionId 后事件覆盖前事件（§4）"
---

<objective>
区身份注入闭环：主路径 additionalContext + /zone 补救换区（PROMPT-01/02/03）。

Purpose: M1 的全部——"区"从标签变成会话里活的行为边界。注入=身份，补救=后悔药。
Output: 两个 hook 的 additionalContext 能力 + /zone skill 文件。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

设计权威：
@DESIGN.md

涉及节：§3（时序+cz 糖糖域）、§4（双机制裁决 A-2、M-3 换区语义）、§2.1（prompt 定稿）、§9（hooks 清单）、V-5/V-6/V-9 实验结论。
</context>

<tasks>

<task type="auto">
  <name>Task 1: zone _zone-prompt 内部子命令</name>
  <files>bin/zone</files>
  <read_first>DESIGN.md §4（M-5）、02-01-SUMMARY.md</read_first>
  <action>
`zone _zone-prompt <zone>`：输出该区合并定义的 prompt 全文（经 02-01 的 show 合并管线，不重复实现）。--json 时 data={zone, prompt}。这是 hook 注入的唯一 prompt 来源（M-5：hook 不藏 prompt 副本）。
  </action>
  <verify>python3 bin/zone _zone-prompt core | head -5（应见 §2.1 core 段开头）；再对 discuss 验证</verify>
  <acceptance_criteria>
    - "_zone-prompt core 输出与 zones/core.yaml prompt 字段（合并后）一致"
    - "未知 zone 名 exit 2"
  </acceptance_criteria>
  <done>prompt 单一来源就位</done>
</task>

<task type="auto">
  <name>Task 2: SessionStart additionalContext 注入</name>
  <files>hooks/session-start.py, hooks/hooks.json</files>
  <read_first>DESIGN.md §3（启动时序）、§9、V-6（12k 注入完整）</read_first>
  <action>
扩展 01-02 的 session-start.py：登记后若解析出门区，除 stdout 提示行外，输出 SessionStart 的 hookSpecificOutput.additionalContext JSON（内容=zone prompt 全文，来源 `zone _zone-prompt`）。unzoned 会话仍零输出（不注入任何东西）。stdout 提示行保留（提示≠注入双轨）。
  </action>
  <verify>printf '{"session_title":"--core 注入验证","source":"startup","cwd":"…"}' | python3 hooks/session-start.py → stdout 应含 additionalContext JSON 且其中 prompt 含 '核心区'</verify>
  <acceptance_criteria>
    - "带前缀 stdin：输出含 additionalContext（prompt=core 区全文）"
    - "无前缀 stdin：无 additionalContext、无提示"
    - "additionalContext 长度与 12k 余量兼容（V-6 已验上限远大于四段 prompt 总量）"
  </acceptance_criteria>
  <done>主路径身份注入可用</done>
</task>

<task type="auto">
  <name>Task 3: UserPromptSubmit hook（/zone 换区）+ skill 文件</name>
  <files>hooks/user-prompt-submit.py, hooks/hooks.json, skills/zone/SKILL.md</files>
  <read_first>DESIGN.md §4 全节、V-5、A-2、M-3</read_first>
  <action>
hook 脚本（薄封装）：读 stdin → prompt 匹配 `^/zone\s+(chore|core|discuss|maint)$` → 不匹配则 exit 0 静默（不侦察普通消息）。匹配则：(a) 调 `zone _zap-title`（bin 内部子命令，本 task 加）：从 transcript 首条用户消息取名称（~40 字去换行；JSON 里 transcript_path 指向 jsonl，解析首条 user 消息；取不到用日戳 fallback），归一化后产出修订 title 并追加 source:command 索引事件；(b) 输出 UserPromptSubmit 的 additionalContext（zone prompt）+ 未消费 handoff 提示行（调 `zone pending`，空则省略）。
不 block prompt（V-9 裁决：A-2 默认即最终形态）。hooks.json 接线。
skills/zone/SKILL.md：占位正文一行（"本命令已由 hook 处理，无需操作。"）+ frontmatter（name: zone）。V-5 已验：文件存在是触发前提，非观感。
  </action>
  <verify>printf '{"prompt":"/zone core","session_id":"t2","transcript_path":"<构造临时 jsonl>","cwd":"…"}' | python3 hooks/user-prompt-submit.py → 输出 additionalContext + 索引出现 source:command 事件；构造 transcript jsonl 验证取题逻辑；'普通消息' 输入 → 静默</verify>
  <acceptance_criteria>
    - "/zone core 输入触发换区（索引事件 source:command）+ prompt 注入"
    - "普通消息静默"
    - "transcript 取名 ~40 字截断生效"
    - "skills/zone/SKILL.md 存在且为一行占位"
  </acceptance_criteria>
  <done>补救路径双机制（hook+skill）齐备</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 主路径注入：echo stdin 测 SessionStart additionalContext
- [ ] 换区：/zone core 触发全套（title+事件+注入）
- [ ] 普通消息零打扰
- [ ] 真会话一次端到端（claude -n "--core m1验收" 后 /zone discuss）
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- hook 无业务逻辑副本（prompt/逻辑全走 bin）
</success_criteria>

<output>
After completion, create `.planning/phases/02-m1-injection/02-02-SUMMARY.md`
</output>
