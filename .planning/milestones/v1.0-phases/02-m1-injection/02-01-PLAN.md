---
phase: 02-m1-injection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/zone
files_deleted: []
coupling_justified: []
autonomous: true
requirements: [ZONE-02]
user_setup: []
must_haves:
  truths:
    - "zone show core 输出合并后 yaml 全文（repo 无覆盖层时=内置默认原样）"
    - "repo 放 .claude/zones/core.yaml 覆盖层后，zone show core 反映覆盖值（字段级 merge，未覆盖字段保留默认）"
    - "repo 覆盖层出现第五区名（如 wow.yaml）→ zone doctor 报警告（非静默，P-6）"
    - "覆盖层含未知字段 → doctor 报错、zone show exit 4（B-6）"
    - "zone doctor 四定义全绿时输出 OK"
  artifacts:
    - bin/zone（show/doctor 实装）
  key_links:
    - "yaml 解析自实现（纯标准库无 PyYAML 依赖）：zones/*.yaml 用受限 schema 子集（扁平键+列表+块标量），解析器约 60-80 行"
---

<objective>
zone.yaml 体系：show 合并视图 + doctor 校验（ZONE-02）。

Purpose: 区定义是护栏与注入的配置源；没有合并与校验机制，覆盖层就是无类型深渊。
Output: bin/zone 的 show/doctor 实装 + 受限 yaml 解析器。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

设计权威：
@DESIGN.md

涉及节：§8（覆盖式合并 Q-4、B-4/B-6 裁决、H-1 值表）、§8.2（黑名单）、P-6（封闭集合警告）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 受限 YAML 解析器</name>
  <files>bin/zone</files>
  <read_first>DESIGN.md §8、zones/*.yaml（Phase 1 产物）</read_first>
  <action>
在 bin/zone 内实现 parse_zone_yaml(text)：支持 flat key、嵌套一层 dict（inject_on_entry、guards）、字符串列表、块标量 prompt（| 语法）。不支持锚点/多文档等高级特性——遇到解析不了的行抛 ZoneDefError（后面 doctor 消费）。理由：纯标准库约束（用户环境无 PyYAML 保证），zone.yaml schema 是受限子集，60-80 行解析器足够。
  </action>
  <verify>python3 -c "import sys; sys.argv=['zone','show','core']; exec(open('bin/zone').read())" 或直接 zone show core；对四个 zones/*.yaml 逐一解析成功</verify>
  <acceptance_criteria>
    - "四区内置 yaml 全部解析成功"
    - "块标量 prompt 保留换行"
  </acceptance_criteria>
  <done>解析器可读全部 Phase 1 产物</done>
</task>

<task type="auto">
  <name>Task 2: zone show（合并视图）</name>
  <files>bin/zone</files>
  <read_first>DESIGN.md §8（合并方向 repo 覆盖插件默认）</read_first>
  <action>
`zone show <zone>`：载入内置 zones/<zone>.yaml → 若 repo 存在 .claude/zones/<zone>.yaml 则字段级深合并（dict 递归、标量与列表整体替换）→ 输出合并后全文。非法定义（解析失败/未知字段）exit 4 并 stderr 说明。--json 时 data={zone_yaml}。
  </action>
  <verify>mkdir -p .claude/zones && cp zones/core.yaml .claude/zones/ 后改 model_hint 字段 → zone show core 反映新值、其余字段不变；rm 恢复（或 git clean）</verify>
  <acceptance_criteria>
    - "无覆盖层：show core == 内置 core.yaml 内容"
    - "有覆盖层：只覆盖字段变化"
    - "未知字段覆盖：exit 4"
  </acceptance_criteria>
  <done>合并语义与 B-6 裁决一致</done>
</task>

<task type="auto">
  <name>Task 3: zone doctor（四类检查）</name>
  <files>bin/zone</files>
  <read_first>DESIGN.md §8（B-6 未知字段不静默）、P-6（第五区警告）</read_first>
  <action>
doctor 检查：(a) 四内置定义可解析且字段合法（prefix 形如 --<name> 与 name 一致、guards 值域合法）；(b) repo 覆盖层只含已知四区文件名——出现第五区名 → 问题清单列出（P-6 非静默警告）；(c) 覆盖层未知字段 → 错误；(d) 输出 OK 或问题清单（file/field/message 结构，附录 A doctor data 契约）。（孤儿/幽灵/索引一致性检查属 Phase 3 的 03-03，本 plan 不做。）
  </action>
  <verify>python3 bin/zone doctor（初始应 OK）；放置 .claude/zones/wow.yaml → doctor 报警告；放置含未知字段的 core.yaml → doctor 报错</verify>
  <acceptance_criteria>
    - "初始 doctor 输出 OK"
    - "第五区名触发问题清单"
    - "未知字段触发问题且 exit 语义正确（附录 A：doctor 发现问题不是 exit 4，exit 4 留给 show 的非法定义；doctor 始终 exit 0 数据里见问题）——照附录 A 表详核"
  </acceptance_criteria>
  <done>doctor 三态（OK/警告/错误）符合 Q-4/P-6/B-6</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] zone show core/discuss/chore/maint 全部可用
- [ ] 覆盖层 merge 行为正确
- [ ] doctor 三态正确
- [ ] 解析器无第三方依赖
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- 无 PyYAML import
</success_criteria>

<output>
After completion, create `.planning/phases/02-m1-injection/02-01-SUMMARY.md`
</output>
